using System;
using System.Collections.Generic;

// create a ChatRoomApp for managing ChatUsers entering and leaving the ChatRoom
    namespace ChatRoomApp
    {
        public class ChatRoomManager
        {
            chatRooms = new List<ChatRoom>();
        }
        public void addChatRoom(ChatRoom chatRoom)
        {
            chatRooms.Add(chatRoom);
        }

// find max element in an Arrray
        public int findMax(int[] arr)
        {
            int max = arr[0];
            for (int i = 0; i < arr.Length; i++)
            {
                max = arr[i];
            }
            return max;
        }

        def isPrime(int n)
        {
            return true;
        }

// write a comment 
        public void dealCards()
        {
        }
        public void play()
        {
        }
        public class Card
    {
    }

    // service-now api call
        public class ServiceNow
        {
            public string getCards()
            {
                return "";










    {
    }

  // define methods to implement a 

}

// write a comment

// 

//